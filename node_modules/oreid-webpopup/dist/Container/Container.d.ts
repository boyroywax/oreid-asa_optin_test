import { WebWidgetCore } from '../WebWidgetCoreHandler/loadWebWidgetCore';
export declare class Container {
    private containerId;
    private iframeId;
    private webWindow;
    constructor({ containerId, iframeId, webWindow, webWidget, }: {
        containerId: string;
        iframeId: string;
        webWindow: Window;
        webWidget: WebWidgetCore;
    });
    get window(): Window;
    get containerElement(): HTMLDivElement | null;
    getIframeSelector(): string;
    show(): void;
    hide(): void;
}
