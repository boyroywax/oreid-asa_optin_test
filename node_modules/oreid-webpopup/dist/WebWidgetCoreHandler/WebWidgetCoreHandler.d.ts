import { OreId, WebWidgetActionParams } from 'oreid-js';
import { Container } from '../Container';
interface RenderProps {
    actionName: string;
    params: WebWidgetActionParams;
}
export declare class WebWidgetCoreHandler {
    private oreId;
    private webWidget;
    private container;
    private isBusy;
    private willClose;
    constructor({ oreId, webWidget, container }: {
        oreId: OreId;
        webWidget: any;
        container: Container;
    });
    private willCloseListener;
    private open;
    private close;
    private getWebWidgetCoreProps;
    render<T extends any>(renderProps: RenderProps): Promise<T>;
}
export {};
